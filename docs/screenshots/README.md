# Screenshots

Drop portfolio screenshots here so the main README can show them. Suggested set
(the filenames the README already links to):

| File | What to capture |
|------|-----------------|
| `plan.png` | The **Plan** tab — phase timeline + the current week expanded. |
| `dashboard.png` | The **Dashboard** tab — load readout + the charts. |
| `strength.png` | The **Strength** tab — the two condensed sessions. |
| `coach.png` | The **coach** panel mid-conversation (with the `🔧 tools used` line visible). |

Run the app (`npm run dev` from the repo root), open http://localhost:5173, and
screenshot each tab. PNGs around 1400px wide look good on GitHub.
